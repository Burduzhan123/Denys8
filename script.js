let i = 6; 

for (;i <= 1000; i++){
    if(i % 2 === 0);
    var string = `Число ${i} є простим числом`
    console.log(string)
    if(i % 2 !== 0);
    var string = `Число ${i} не є простим числом`
    console.log(string)
}